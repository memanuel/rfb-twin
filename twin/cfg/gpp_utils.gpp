// *********************************************************************************************************************
// Macros to calculate an arithmetic expression
// *********************************************************************************************************************
#define EVAL(expr) #eval expr
#define TRIM(str) #exec echo -n str
#define CALC(expr) TRIM(#exec calc expr)
